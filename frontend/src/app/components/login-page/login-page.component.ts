import { Component } from '@angular/core';

@Component({
  selector: 'app-login-page',
  standalone: true, // Make sure standalone is true if you generated it that way
  imports: [],
  templateUrl: './login-page.component.html', // Corrected path
  styleUrl: './login-page.component.scss'
})
export class LoginPageComponent { // Ensure class name matches export

}